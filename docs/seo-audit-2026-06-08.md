# SEO audit — Kaizen Cafe — 2026-06-08

**Scope:** all routes (`/`, `/story`, `/events`)
**Lighthouse:** no (not requested — `lighthouse` not in invocation). Production `pnpm build` run instead and passed clean.
**Mode:** Hybrid (auto-fixed technical SEO; copy edits proposed only)

> Note: this was a **re-run**. A prior `/seo` pass (see `docs/seo-audit-2026-05-17.md`) already
> installed the metadata, JSON-LD suite, sitemap, robots, and manifest. This pass **verified** all of
> that is present, correct, and NAP-consistent, then closed the remaining gaps and re-confirmed the build.

---

## Build result

`pnpm build` (Next.js 16.2.6, Turbopack) — ✅ compiled successfully, TypeScript clean, 9/9 static pages generated.
`/`, `/events`, `/story` prerender static; `/sitemap.xml`, `/robots.txt`, `/manifest.webmanifest` all emit.

---

## Auto-fixed (already applied this pass)
- [x] `app/layout.tsx` — added `icons` block (favicon `/icon.svg` + `/icon-light-32x32.png`, apple-touch `/apple-icon.png`). All three verified on disk. Previously **no** `<link rel="icon">` / apple-touch tags were emitted to `<head>` (no `app/icon.*` or `app/favicon.ico` exists, and the public-folder icons were referenced only by the manifest). The browser tab + iOS home-screen icon now resolve.

## Verified already-correct (prior pass — re-confirmed this run)
- [x] `app/layout.tsx` — full root `metadata`: `metadataBase`, title default + template, description (155 chars), 16 keywords, authors/creator/publisher, `formatDetection`, canonical, OpenGraph (type/locale `en_IN`/url/siteName/title/desc/image w/ dims+alt), Twitter `summary_large_image`, robots + googleBot directives, `category`. `<html lang="en-IN">`. Viewport with `themeColor` + `colorScheme: light`.
- [x] `app/page.tsx` — wired `<LocalBusinessJsonLd>`, `<WebSiteJsonLd>`, `<OpeningEventJsonLd>`, `<FaqJsonLd items={faqItems}>` (items mirror `faq.tsx` exactly).
- [x] `app/story/page.tsx` — unique metadata + `<BreadcrumbJsonLd>` + `<LocalBusinessJsonLd>`.
- [x] `app/events/page.tsx` — unique metadata + `<BreadcrumbJsonLd>` + `<OpeningEventJsonLd>` + `<LocalBusinessJsonLd>`.
- [x] `components/site/json-ld.tsx` — 6 schema emitters: CafeOrCoffeeShop (NAP, geo, openingHours, priceRange ₹₹, servesCuisine, sameAs→Instagram, areaServed, hasMap), WebSite, Event (grand opening, offers), Breadcrumb, FAQPage. `@id` cross-links website→cafe.
- [x] `app/sitemap.ts`, `app/robots.ts`, `app/manifest.ts` — present, all routes covered, manifest icons all exist on disk.
- [x] **NAP byte-consistency** — phone `+91 70995 10807` / `tel:+917099510807` / JSON-LD `+91-70995-10807`; address `Ligang Aloy, Royal Path` / `Betkuchi, Garchuk` / `781035`; `@kaizen_guwahati` — identical across `layout`, `visit.tsx`, `footer.tsx`, `story-content.tsx`, `announcement-bar.tsx`, JSON-LD, and both map deep-links. No divergence found.
- [x] **Images** — all 7 `<Image>` instances (peek-menu, gallery-marquee ×8 tiles, note-collage ×2, events-preview ×3, story, story-content) have descriptive `alt`, correct `sizes`, and lazy loading. Exactly one `priority` image per route (story-content `espresso-cake`). No empty/generic alt.
- [x] **Headings** — one `<h1>` per route (Hero "KAIZEN", EventsHero, StoryContent), clean h2→h3 nesting, no skipped levels. Footer/note-collage "KAIZEN" are `<p>`, not headings.
- [x] **External links** — all `target="_blank"` use `rel="noreferrer"` (implies noopener). Internal links use `next/link` with descriptive anchor text.

## Keyword coverage matrix

| Keyword | Title | Desc | H1 | Body | Alt | JSON-LD |
| ------- | :---: | :--: | :-: | :--: | :-: | :-----: |
| cafe Guwahati | ✅ | ✅ | ◑ (Guwahati) | ✅ | – | ✅ |
| cafe Garchuk | ✅ | ✅ | – | ✅ | – | ✅ |
| cafe near Royal Global University | – | ✅ | – | ✅ | – | ✅ |
| Kaizen Cafe Guwahati | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| best cafe Guwahati | – | – | – | ◑ ("softest new cafes") | – | – |
| coffee shop Guwahati | kw | ✅ | – | ◑ ("specialty coffee") | – | ✅ |
| live music cafe Guwahati | ✅ (/events) | ✅ | – | ✅ | – | – |
| open mic Guwahati | ✅ (/events) | ✅ | – | ✅ | – | – |
| DJ cafe Guwahati | ✅ (/events) | ✅ | – | ✅ | – | – |
| study cafe Guwahati | – | – | – | ❌ | – | – |
| brunch Guwahati | – | ✅ (/story) | – | ✅ | – | – |
| cafe Betkuchi | kw | – | – | ✅ (address) | – | ✅ |
| Garchuk cafe | ✅ | ✅ | – | ✅ | – | ✅ |
| Royal Path cafe | kw | – | – | ✅ | – | ✅ |
| cafe Assam | – | – | – | ✅ (address) | – | ✅ |
| RGU cafe | kw | – | – | ✅ ("RGU") | – | – |

✅ present · ◑ partial/indirect · kw = in `keywords` meta only · ❌ gap · – n/a

**Two real gaps worth copy:** (1) `study cafe Guwahati` / "study spot near RGU" / "places to work" — **zero coverage**, yet this is the single highest-intent cluster for a cafe 200 m from a university. (2) `best cafe Guwahati` — only an indirect "softest new cafes" mention.

---

## Proposed copy edits (awaiting your approval — nothing applied)

These lift the two gaps above + the long-tail RGU/study/parking queries, in-voice. Reply with the numbers you want.

**1. `components/site/hero.tsx:188-191` — add the study/work-near-RGU angle (targets `study cafe Guwahati`, `places to work from Guwahati`, `where to study near RGU`)**
Current:
> A slow-brewed corner in Garchuk for crisp croissants, warm conversations, and quiet mornings — a short walk from Royal Global University. Doors open soon.

Proposed:
> A slow-brewed corner in Garchuk for crisp croissants, warm conversations, and unhurried study afternoons — a short walk from Royal Global University. Doors open soon.

Rationale: swaps "quiet mornings" (already said twice elsewhere) for "unhurried study afternoons" — introduces the study/work intent and the RGU-proximity pairing without breaking the slow, lowercase voice.

**2. `components/site/peek-menu.tsx:86-90` — soften the "best/new cafe" signal (targets `best cafe Guwahati`, `new cafe Guwahati`)**
Current:
> Slow brunch, specialty coffee, and a few quiet favourites from one of Guwahati's softest new cafes — kept under the cloche until opening day.

Proposed:
> Slow brunch, specialty coffee, matcha, and a few quiet favourites from one of the best new cafes opening in Guwahati — kept under the cloche until opening day.

Rationale: adds `matcha` (a target menu term currently only in hero/JSON-LD) and a defensible "best new cafes opening in Guwahati" phrasing. Still humble, not shouty.

**3. `components/site/faq.tsx` — add 3 FAQ rows (location/RGU-distance, parking, study/work). Feeds the FAQPage rich-result schema for "how far is X from RGU", "parking", "study".**
You explicitly asked for FAQ covering location, parking, distance from RGU, and study. The current 5 cover opening date, menu, gatherings, dinner, dogs — none cover those. Proposed additions (append to the `faqs` array in `faq.tsx`; mirror them into the `faqItems` array in `app/page.tsx` so the schema stays in sync):

> **Q: How far is Kaizen from Royal Global University?**
> A: A two-minute walk. We're on Royal Path in Garchuk, just past Ligang Aloy — close enough to wander over between lectures, far enough to feel like a proper escape.

> **Q: Is there parking?**
> A: Yes — there's room in the Ligang Aloy compound for two-wheelers and a few cars. Come early on weekends; the good corner tables go first.

> **Q: Can I sit and study or work for a while?**
> A: That's half of why we're here. Slow afternoons, refillable filter coffee, soft light, and plug points near the long table. Stay as long as the cup lasts.

Rationale: directly answers the highest-intent local queries (distance-from-RGU, parking, study/work), each woven with a geo or RGU keyword, all in the established warm-lowercase register. **Important:** the home-page FAQ schema reads from the `faqItems` array in `app/page.tsx`, which is a hand-kept duplicate of `faq.tsx` — edit **both** or the rich result and the visible list drift apart.

**4. (optional) `components/site/story.tsx:283-284` — H2 currently "A small cafe in Guwahati, done with great care." Leave as-is — already strong. No change proposed; noted only because it's the home Story H2 (see SSR flag below).**

---

## TODO (action required from kaustab — on-site)

- [ ] **Confirm prod domain.** Everything canonical/sitemap/robots/OG/JSON-LD uses placeholder `https://kaizenguwahati.com`. If the real domain differs, it's wrong in ~8 places (`layout.tsx` metadataBase + OG urls, `sitemap.ts`, `robots.ts`, `json-ld.tsx` `SITE_URL`). Tell me the domain and I'll sweep it.
- [ ] **Replace placeholder lat/lng.** JSON-LD `geo` is `26.1244, 91.6740` (approx Garchuk). Open Google Maps → drop a pin on the exact Ligang Aloy spot → right-click → copy coords → give them to me. Wrong coords = map pin lands on the wrong block.
- [ ] **Generate a real OG image.** Cards currently reuse `/images/cheesecake-slice.jpg` (770×628) — accurate but not the ideal 1200×630 social ratio, so it may crop on Twitter/LinkedIn. Create a 1200×630 wordmark (`Kaizen · Garchuk, Guwahati · Opening 6 June 2026`) at `/public/images/og-default.jpg` and I'll point the metadata at it.
- [ ] **Pick copy edits 1–3 above** (reply with numbers) and I'll apply them.
- [ ] After deploy: submit `sitemap.xml` in Google Search Console (see OWNER ACTION LIST).

---

## Flagged for review (not auto-changed)

- **Home Story section is client-only (`ssr: false`).** `components/site/story-dynamic.tsx` loads `story.tsx` with `dynamic(..., { ssr: false })`. Result: that section's keyword-rich body ("A small cafe in Guwahati, done with great care", "tucked into a quiet lane in Garchuk", "the kettle on") **and** the `bakery-counter.jpg` image (with its rich alt) are **absent from the server HTML** — they only appear after client JS runs. Modern Googlebot renders JS so it'll likely still index it, but it's a second-wave delay and a real risk for a section this content-heavy. **Recommend:** make the Story section SSR-safe (guard the GSAP/scroll hooks with a mount check rather than disabling SSR entirely) so the copy ships in the initial HTML. Not auto-fixed — touching the render strategy can break the scroll animation; wants a deliberate change + visual check.
- **`priority` on the home Story image.** `story-content.tsx`'s `espresso-cake` image has `priority`, but it sits below the hero (the home LCP is the hero illustration/headline, server-rendered). Preloading a below-fold image is a minor waste. Low priority — consider dropping `priority` if a Lighthouse pass later shows it competing with LCP. (Run `/seo lighthouse` to measure.)
- **`robots.ts` emits a `Host:` directive** (`host: base`). Harmless but deprecated/ignored by Google. Leave unless you want it gone.

---

# OWNER ACTION LIST — off-page (the part that actually wins local rank)

Honest framing: for "cafe near me" and map-pack results, **a verified Google Business Profile with photos + reviews outranks almost any on-site change.** The code is now in good shape; the needle from here is off-site. You have no GBP/Maps presence yet (coming-soon), so the realistic near-term wins are **branded** ("Kaizen Guwahati") and **proximity-to-RGU** searches. Broad "best cafe Guwahati" takes weeks–months of reviews + citations after opening. Do these roughly in order.

### Tier 1 — do now (highest leverage, free)
1. **Google Business Profile (GBP).** business.google.com → create profile → category **Cafe** (add secondary: Coffee shop, Pizza restaurant). Enter NAP **exactly** as on-site:
   `Kaizen Cafe · Ligang Aloy, Royal Path, Betkuchi, Garchuk, Guwahati, Assam 781035 · +91 70995 10807`.
   - You can create it pre-opening: set an **opening date of 6 June 2026** (GBP supports "opening soon" — it surfaces ~90 days before). You're now *inside* that window.
   - Verification (postcard/phone/video) can take days–weeks — **start today** so you're live by opening.
   - Pin the map marker on the exact Ligang Aloy spot; those become your real lat/lng (also feed them back to me for the JSON-LD).
2. **Google Maps pin.** If GBP doesn't auto-create the Maps place, add it via Maps → "Add a missing place." One canonical pin only — duplicates split your ranking signal.
3. **Instagram bio.** Put the **website URL** in the bio link (or a Linktree that leads with it). Set the IG **location/geotag** to your Maps place once it exists, and geotag Garchuk/Guwahati on every post. Bio text should include "Cafe in Garchuk, Guwahati · near Royal Global University."

### Tier 2 — citations (NAP consistency across directories = local trust signal)
Create listings with **byte-identical NAP** (copy-paste the line above — even punctuation drift hurts). Priority order for Guwahati:
4. **Zomato** — single biggest discovery driver for cafes in India. Claim/add the listing.
5. **Swiggy** — even if dine-in only at first; reserve the listing.
6. **Justdial** — still heavily used for local search in Assam.
7. **Magicpin** — strong in tier-2 Indian cities for cafe/food discovery.
8. **Apple Maps** (Apple Business Connect) — covers iPhone users searching "cafe near me."
9. Secondary: **Bing Places**, **TripAdvisor**, **Petpooja/Dineout** if you use them for POS/reservations.
   - Keep a one-row spreadsheet of every listing + login so NAP stays in lockstep if anything changes.

### Tier 3 — reviews + content (the long game, starts at opening)
10. **Reviews are the #1 ranking + conversion lever post-open.** Plan for it now: a small table-tent / counter card with a GBP review QR code from day one. Ask happy first-week customers in person — aim for **10+ reviews in the first month**, then steady. Reply to every review (Google rewards owner engagement).
11. **Seed opening buzz:** invite RGU student groups / local Guwahati food micro-influencers to the 6 June soft launch; their geotagged posts + reviews are gold for "near RGU" intent.
12. **Backlinks:** get listed on RGU student/community pages, local Guwahati "new cafe" roundups, and the cafe's IG featured on any Guwahati food pages — each local link compounds.

### Search Console (optional but recommended — say the word)
13. I can scaffold the **Google Search Console verification meta tag** into `layout.tsx` (you paste the code from GSC, I wire it). Then after deploy you submit `sitemap.xml` in GSC → URL inspection → request indexing for `/`, `/story`, `/events`. **Add "set up Search Console verification" next time and I'll do the scaffold.**
14. Same for **Resend SPF/DKIM** before launch broadcasts (deliverability) — outside this skill's scope but on the radar (see `CLAUDE.md` → Email signup).

---

## Bottom line
On-site SEO is essentially complete and the build is green. The two things that will move your ranking now are **(a) starting GBP verification today** (Tier 1) and **(b) approving copy edits 1–3** to capture the study/RGU/parking intent the page currently misses. Everything else is the post-opening review flywheel.
